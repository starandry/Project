export type ScrollButtonProps = {
    threshold?: number; // после какого скролла показывать кнопку
    offset?: number; // насколько близко к низу считается "внизу"
    upLabel?: string; // текст для скролла вверх
    downLabel?: string; // текст для скролла вниз
};
